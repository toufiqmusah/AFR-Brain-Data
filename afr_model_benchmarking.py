{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"dockerImageVersionId":28755,"isInternetEnabled":false,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:42:25.940876Z\",\"iopub.execute_input\":\"2026-06-26T16:42:25.941497Z\",\"iopub.status.idle\":\"2026-06-26T16:42:34.592351Z\",\"shell.execute_reply.started\":\"2026-06-26T16:42:25.941470Z\",\"shell.execute_reply\":\"2026-06-26T16:42:34.591373Z\"}}\nimport torch\nprint(torch.__version__)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:42:41.120503Z\",\"iopub.execute_input\":\"2026-06-26T16:42:41.121269Z\",\"iopub.status.idle\":\"2026-06-26T16:43:03.433302Z\",\"shell.execute_reply.started\":\"2026-06-26T16:42:41.121201Z\",\"shell.execute_reply\":\"2026-06-26T16:43:03.432203Z\"}}\n# torchio handles NIfTI loading and 3D preprocessing\n# monai ViT is used as the PRIMUS encoder stand-in (same ViT-B/16^3 architecture)\n# huggingface_hub downloads the checkpoint\n!pip install torchio pandas monai huggingface_hub nnunetv2 safetensors scikit-learn\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:43:14.665734Z\",\"iopub.execute_input\":\"2026-06-26T16:43:14.666712Z\",\"iopub.status.idle\":\"2026-06-26T16:44:17.226184Z\",\"shell.execute_reply.started\":\"2026-06-26T16:43:14.666669Z\",\"shell.execute_reply\":\"2026-06-26T16:44:17.225341Z\"}}\nfrom __future__ import annotations\nfrom pathlib import Path\nimport re\nimport numpy as np\nimport pandas as pd\nimport torch\nimport torch.nn as nn\nimport torchio as tio\n\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import balanced_accuracy_score, f1_score\nfrom torch.utils.data import WeightedRandomSampler\nfrom huggingface_hub import hf_hub_download\n\n# MONAI ViT — same architecture as PRIMUS encoder (ViT-B/16^3)\n# We use this because PRIMUS encoder IS a plain 3D ViT-B\n# Input: (B, 1, 96, 96, 96)  Output: (sequence, hidden_states)\nfrom monai.networks.nets import ViT\n\nprint(\"Imports OK\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:23.073025Z\",\"iopub.execute_input\":\"2026-06-26T16:44:23.073831Z\",\"iopub.status.idle\":\"2026-06-26T16:44:23.082467Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:23.073740Z\",\"shell.execute_reply\":\"2026-06-26T16:44:23.081524Z\"}}\nDATA_ROOT = \"/kaggle/input/datasets/sparkedwakanda26/skull-stripped-mri/skull_stripped_data\"\nTSV_PATH = \"/kaggle/input/datasets/tobi24/participant-tsv/participant-info.tsv\"\nTARGET_SIZE = (96, 96, 96)\nLABEL_MAP = {\"Control\": 0, \"Dementia\": 1, \"Parkinson\": 2}\nRANDOM_SEED = 42\nMODALITY_KEY = \"dt-neuro-anat-t2w\" \n\nPRIMUS_CKPT_PATH = \"/kaggle/working/primus_weights/checkpoint_final.pth\"\n\n\n\n\nIN_CHANNELS  = 1\nPATCH_SIZE   = (8, 8, 8)\nHIDDEN_SIZE  = 864\nMLP_DIM      = 2304\nNUM_LAYERS   = 16\nNUM_HEADS    = 16\n\n\n\n\n\n\n\n\n# ── Training ──────────────────────────────────────────────────────────────────\nRANDOM_SEED  = 42\nN_FOLDS      = 5\nBATCH_SIZE   = 8\n\n# ── PRIMUS checkpoint ─────────────────────────────────────────────────────────\n# PRIMUS weights are distributed inside nnUNet v2.\n# After installing nnunetv2, download the checkpoint with:\n#   nnUNetv2_download_pretrained_model_by_url <url>\n# OR point PRIMUS_CKPT_PATH to a .pth file you downloaded manually.\n# Set this to the actual path after downloading.\nPRIMUS_CKPT_PATH = \"/kaggle/input/primus-weights/primus_encoder.pth\"  # <-- UPDATE THIS\n\nprint(\"Configuration loaded\")\nprint(f\"  Dataset : {DATA_ROOT}\")\nprint(f\"  Volume  : {TARGET_SIZE}\")\nprint(f\"  ViT     : {NUM_LAYERS} layers x {NUM_HEADS} heads, hidden={HIDDEN_SIZE}\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:27.742715Z\",\"iopub.execute_input\":\"2026-06-26T16:44:27.743441Z\",\"iopub.status.idle\":\"2026-06-26T16:44:36.939886Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:27.743410Z\",\"shell.execute_reply\":\"2026-06-26T16:44:36.939056Z\"}}\nfrom huggingface_hub import hf_hub_download\n\npath = hf_hub_download(\n    repo_id=\"MIC-DKFZ/PrimusM-OpenMind-MAE\",\n    filename=\"checkpoint_final.pth\",\n    local_dir=\"./primus_weights\"\n)\nprint(f\"Downloaded to: {path}\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:40.221102Z\",\"iopub.execute_input\":\"2026-06-26T16:44:40.221411Z\",\"iopub.status.idle\":\"2026-06-26T16:44:40.239238Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:40.221383Z\",\"shell.execute_reply\":\"2026-06-26T16:44:40.238435Z\"}}\ntorch.manual_seed(RANDOM_SEED)\nnp.random.seed(RANDOM_SEED)\nprint('Seed set')\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:45.072479Z\",\"iopub.execute_input\":\"2026-06-26T16:44:45.073290Z\",\"iopub.status.idle\":\"2026-06-26T16:44:45.428198Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:45.073251Z\",\"shell.execute_reply\":\"2026-06-26T16:44:45.427409Z\"}}\n!wget -O /kaggle/working/participant-info.tsv \\\n  \"https://raw.githubusercontent.com/toufiqmusah/AFR-Brain-Data/main/participant-info.tsv\"\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:51.156315Z\",\"iopub.execute_input\":\"2026-06-26T16:44:51.156649Z\",\"iopub.status.idle\":\"2026-06-26T16:44:51.193184Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:51.156612Z\",\"shell.execute_reply\":\"2026-06-26T16:44:51.192380Z\"}}\nTSV_PATH = \"/kaggle/working/participant-info.tsv\"\n\n# ── Load participant labels ───────────────────────────────────────────────────\ndemo = pd.read_csv(TSV_PATH, sep=\"\\t\")\ndemo.columns = demo.columns.str.strip()\n\n# Handle both spelling variants seen in the actual TSV\nlabel_col = \"ClincalGroup\"\nif label_col not in demo.columns:\n    label_col = \"ClinicalGroup\"\n    if label_col not in demo.columns:\n        raise KeyError(\n            f\"Neither ClincalGroup nor ClinicalGroup found in TSV. \"\n            f\"Available columns: {list(demo.columns)}\"\n        )\n\nlabel_lookup = {}\nfor _, row in demo.iterrows():\n    subj_num = int(row[\"Subject\"])\n    label_lookup[subj_num] = LABEL_MAP[row[label_col]]\n\nprint(f\"TSV loaded from   : {TSV_PATH}\")\nprint(f\"Label column used : '{label_col}'\")\nprint(f\"Subjects loaded   : {len(label_lookup)}\")\nprint(f\"Class mapping     : {LABEL_MAP}\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:44:54.505935Z\",\"iopub.execute_input\":\"2026-06-26T16:44:54.506744Z\",\"iopub.status.idle\":\"2026-06-26T16:45:00.741842Z\",\"shell.execute_reply.started\":\"2026-06-26T16:44:54.506708Z\",\"shell.execute_reply\":\"2026-06-26T16:45:00.741084Z\"}}\nroot = Path(DATA_ROOT)\nsubjects = []\nmissing_mods = []\nsubject_mods = []\nall_subject_data = []\n\n\nfor subj_dir in sorted(root.glob(\"sub-*\")):\n    subj_num = int(subj_dir.name.split(\"-\")[1])\n    label = label_lookup.get(subj_num)\n    if label is None:\n        missing_mods.append(subj_dir.name)\n        continue\n\n    # collect all modalities for this subject\n    mod_dirs = sorted(subj_dir.iterdir())\n    mod_images = {}\n    for mod_dir in mod_dirs:\n        if not mod_dir.is_dir():\n            continue\n        nii_files = sorted(mod_dir.glob(\"*.nii\"))\n        if nii_files:\n            # strip random suffix (\n            mod_key = re.sub(r'\\.id-[^.]+$', '', mod_dir.name)\n            mod_images[mod_key] = tio.ScalarImage(str(nii_files[0]))\n\n    if not mod_images:\n        missing_mods.append(subj_dir.name)\n        continue\n    subject_mods.append(set(mod_images))\n    all_subject_data.append((mod_images, label, subj_dir.name))\n\n# find modality keys common to ALL subjects\ncommon_mods = sorted(set.intersection(*subject_mods)) if subject_mods else []\nprint(f\"Modality keys common to all {len(subject_mods)} subjects: {common_mods}\")\n\nif not common_mods:\n    print(\"WARNING: no common modalities -- using single-modality fallback\")\n\n# for mod_images, label, sid in all_subject_data:\n#     if common_mods:\n#         ref = mod_images[common_mods[0]]\n#         aligned = [ref]\n#         for name in common_mods[1:]:\n#             if name in mod_images:\n#                 aligned.append(tio.Resample(ref)(mod_images[name]))\n#             else:\n#                 aligned.append(tio.ScalarImage(tensor=torch.zeros_like(ref.data), affine=ref.affine))\n#         stacked_data = torch.cat([a.data for a in aligned], dim=0)\n#         stacked_img = tio.ScalarImage(tensor=stacked_data, affine=ref.affine)\n#     else:\n#         # single modality (first available)\n#         first = next(iter(mod_images.values()))\n#         stacked_img = tio.ScalarImage(tensor=first.data, affine=first.affine)\n\n#     subj = tio.Subject(\n#         img=stacked_img,\n#         diagnosis=label,\n#         subject_id=sid,\n#     )\n#     subjects.append(subj)\n\nfor mod_images, label, sid in all_subject_data:\n    if MODALITY_KEY not in mod_images:\n        missing_mods.append(sid)\n        continue\n    img = mod_images[MODALITY_KEY] \n    subj = tio.Subject(img=img, diagnosis=label, subject_id=sid)\n    subjects.append(subj)\n\n\nprint(f\"Subjects: {len(subjects)}, no-label/missing: {len(missing_mods)}\")\nif subjects:\n    print(f\"Channels: {subjects[0].img.shape[0]}, shape: {list(subjects[0].img.shape)}\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:45:22.053074Z\",\"iopub.execute_input\":\"2026-06-26T16:45:22.053932Z\",\"iopub.status.idle\":\"2026-06-26T16:45:22.224121Z\",\"shell.execute_reply.started\":\"2026-06-26T16:45:22.053899Z\",\"shell.execute_reply\":\"2026-06-26T16:45:22.223133Z\"}}\n# TorchIO preprocessing transform\n# Identical to BrainIAC notebook - keeps the benchmark comparison fair\ntransform = tio.Compose([\n    tio.RescaleIntensity(out_min_max=(0, 1)),\n    tio.CropOrPad(TARGET_SIZE),\n])\n\n# Stratified train / held-out test split (88% / 12%)\nall_labels = torch.tensor([s.diagnosis for s in subjects])\nclasses    = torch.unique(all_labels)\nrng        = torch.Generator().manual_seed(RANDOM_SEED)\n\ntrain_idx, test_idx = [], []\nfor c in classes:\n    idx    = torch.where(all_labels == c)[0]\n    idx    = idx[torch.randperm(len(idx), generator=rng)]\n    n_test = max(1, round(len(idx) * 0.12))\n    test_idx.extend(idx[:n_test].tolist())\n    train_idx.extend(idx[n_test:].tolist())\n\ntrain_val_subjs = [subjects[i] for i in train_idx]\ntest_subjs      = [subjects[i] for i in test_idx]\n\n# Held-out test loader\ntest_ds     = tio.SubjectsDataset(test_subjs, transform=transform)\ntest_loader = tio.SubjectsLoader(\n    test_ds, batch_size=BATCH_SIZE, shuffle=False, num_workers=0, pin_memory=False\n)\nprint(f\"Held-out test : {len(test_subjs)} subjects  ({len(test_loader)} batches)\")\n\n# 5-fold CV index bookkeeping\nall_cv_idx = torch.tensor(train_idx)\nperm       = all_cv_idx[torch.randperm(len(all_cv_idx), generator=rng)]\nfold_size  = len(perm) // N_FOLDS\n\nfor fold in range(N_FOLDS):\n    val_start  = fold * fold_size\n    val_end    = val_start + fold_size if fold < N_FOLDS - 1 else len(perm)\n    val_idx    = perm[val_start:val_end].tolist()\n    tr_idx     = torch.cat([perm[:val_start], perm[val_end:]]).tolist()\n    fold_train = [subjects[i] for i in tr_idx]\n    fold_val   = [subjects[i] for i in val_idx]\n\n    fold_labels  = [s.diagnosis for s in fold_train]\n    class_counts = torch.bincount(torch.tensor(fold_labels))\n    weights      = 1.0 / class_counts.float()\n    sampler      = WeightedRandomSampler(\n        weights[torch.tensor(fold_labels)], len(fold_train), replacement=True\n    )\n    train_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_train, transform=transform),\n        batch_size=BATCH_SIZE, sampler=sampler, num_workers=0, pin_memory=False\n    )\n    val_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_val, transform=transform),\n        batch_size=BATCH_SIZE, shuffle=False, num_workers=0, pin_memory=False\n    )\n    print(f\"Fold {fold+1}: train={len(fold_train)}  val={len(fold_val)}\")\n\nprint(\"\\nDataloaders ready.\")\nprint(f\"Test loader : {len(test_loader)} batches ({len(test_subjs)} samples)\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:45:27.899677Z\",\"iopub.execute_input\":\"2026-06-26T16:45:27.900453Z\"}}\nimport torch\n\nPRIMUS_CKPT_PATH = \"/kaggle/working/primus_weights/checkpoint_final.pth\"\nprint(f\"Loading checkpoint from: {PRIMUS_CKPT_PATH}\")\nckpt = torch.load(PRIMUS_CKPT_PATH, map_location=\"cpu\")\n\n# Show top-level keys and their types\nprint(\"\\nTop-level checkpoint keys:\")\nfor k in ckpt.keys():\n    v = ckpt[k]\n    if isinstance(v, dict):\n        print(f\"  '{k}'  ->  dict with {len(v)} entries\")\n    elif isinstance(v, torch.Tensor):\n        print(f\"  '{k}'  ->  Tensor {tuple(v.shape)}\")\n    else:\n        print(f\"  '{k}'  ->  {type(v).__name__}\")\n\n# Drill into the actual weights\nif \"state_dict\" in ckpt:\n    state_dict = ckpt[\"state_dict\"]\n    print(\"\\nUsing key: 'state_dict'\")\nelif \"model\" in ckpt:\n    state_dict = ckpt[\"model\"]\n    print(\"\\nUsing key: 'model'\")\nelif \"model_state_dict\" in ckpt:\n    state_dict = ckpt[\"model_state_dict\"]\n    print(\"\\nUsing key: 'model_state_dict'\")\nelse:\n    state_dict = ckpt\n    print(\"\\nWeights at top level (no wrapper)\")\n\n# Separate tensor keys from non-tensor keys\ntensor_keys = {k: v for k, v in state_dict.items() if isinstance(v, torch.Tensor)}\nother_keys  = {k: v for k, v in state_dict.items() if not isinstance(v, torch.Tensor)}\n\nprint(f\"\\nTotal keys in state dict : {len(state_dict)}\")\nprint(f\"  Tensor keys            : {len(tensor_keys)}\")\nprint(f\"  Non-tensor keys        : {len(other_keys)}\")\n\nif other_keys:\n    print(\"\\nNon-tensor keys (skipping shape print for these):\")\n    for k, v in other_keys.items():\n        print(f\"  '{k}'  ->  {type(v).__name__}  =  {v}\")\n\nprint(\"\\nFirst 30 TENSOR keys (these are the actual weights):\")\nfor k in list(tensor_keys.keys())[:30]:\n    print(f\"  {k}  ->  shape {tuple(tensor_keys[k].shape)}\")\n\nprint(\"\\nUnique prefixes (everything before the first dot):\")\nprefixes = sorted(set(k.split('.')[0] for k in tensor_keys.keys()))\nprint(\" \", prefixes)\n\nprint(\"\\n>>> Look at the prefixes above.\")\nprint(\">>> The encoder prefix is what you set as ENCODER_PREFIX in Cell 9.\")\nprint(\">>> Common values: 'encoder', 'backbone', 'student', 'network'\")\n\n# Update state_dict to only contain tensors going forward\nstate_dict = tensor_keys\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:48:34.821711Z\",\"iopub.execute_input\":\"2026-06-26T16:48:34.822081Z\",\"iopub.status.idle\":\"2026-06-26T16:48:34.830677Z\",\"shell.execute_reply.started\":\"2026-06-26T16:48:34.822053Z\",\"shell.execute_reply\":\"2026-06-26T16:48:34.829852Z\"}}\n# The weights are inside 'network_weights' - extract them directly\nstate_dict = ckpt[\"network_weights\"]\n\nprint(f\"Extracted 'network_weights': {len(state_dict)} entries\")\n\n# Separate tensor keys from non-tensor keys\ntensor_keys = {k: v for k, v in state_dict.items() if isinstance(v, torch.Tensor)}\nother_keys  = {k: v for k, v in state_dict.items() if not isinstance(v, torch.Tensor)}\n\nprint(f\"\\nTensor keys  : {len(tensor_keys)}\")\nprint(f\"Other keys   : {len(other_keys)}\")\n\n# Print first 30 tensor keys so you can see the prefix structure\nprint(\"\\nFirst 30 TENSOR keys:\")\nfor k in list(tensor_keys.keys())[:30]:\n    print(f\"  {k}  ->  shape {tuple(tensor_keys[k].shape)}\")\n\n# Show all unique prefixes - this tells you what ENCODER_PREFIX to use in Cell 9\nprint(\"\\nUnique prefixes (everything before the first dot):\")\nprefixes = sorted(set(k.split('.')[0] for k in tensor_keys.keys()))\nprint(\" \", prefixes)\n\n# Also print the extra metadata so you understand what the checkpoint contains\nprint(\"\\nExtra metadata in checkpoint:\")\nprint(f\"  trainer_name         : {ckpt['trainer_name']}\")\nprint(f\"  init_args keys       : {list(ckpt['init_args'].keys())}\")\nprint(f\"  nnssl_adaptation_plan: {list(ckpt['nnssl_adaptation_plan'].keys())}\")\nprint(f\"  citations            : {ckpt['citations']}\")\n\n# Update state_dict to only tensor keys going forward\nstate_dict = tensor_keys\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:48:59.047544Z\",\"iopub.execute_input\":\"2026-06-26T16:48:59.048434Z\",\"iopub.status.idle\":\"2026-06-26T16:48:59.055824Z\",\"shell.execute_reply.started\":\"2026-06-26T16:48:59.048400Z\",\"shell.execute_reply\":\"2026-06-26T16:48:59.054971Z\"}}\n# The encoder in this PRIMUS checkpoint is the EVA backbone\n# Prefix is \"eva.\" based on the keys we saw in Cell 8\nENCODER_PREFIX = \"eva.\"\n\n# Also extract the stem (patch embedding) separately\nSTEM_PREFIX = \"down_projection.\"\n\n# Extract EVA encoder weights - strip the \"eva.\" prefix\nencoder_state_dict = {\n    k[len(ENCODER_PREFIX):]: v\n    for k, v in state_dict.items()\n    if k.startswith(ENCODER_PREFIX)\n}\n\n# Extract stem weights separately\nstem_state_dict = {\n    k[len(STEM_PREFIX):]: v\n    for k, v in state_dict.items()\n    if k.startswith(STEM_PREFIX)\n}\n\nprint(f\"Total keys in checkpoint      : {len(state_dict)}\")\nprint(f\"EVA encoder keys extracted    : {len(encoder_state_dict)}\")\nprint(f\"Stem (down_projection) keys   : {len(stem_state_dict)}\")\nprint(f\"Decoder/other keys (discarded): \"\n      f\"{len(state_dict) - len(encoder_state_dict) - len(stem_state_dict)}\")\n\nprint(\"\\nFirst 10 extracted EVA keys (no 'eva.' prefix):\")\nfor k in list(encoder_state_dict.keys())[:10]:\n    print(f\"  {k}  ->  {tuple(encoder_state_dict[k].shape)}\")\n\n# Key architecture facts from the checkpoint\nprint(\"\\nArchitecture facts from checkpoint:\")\nprint(f\"  hidden_size  : 864  (from weight shapes)\")\nprint(f\"  patch_size   : 8x8x8  (from down_projection.proj.weight shape (864,1,8,8,8))\")\nprint(f\"  pos_embed    : {tuple(encoder_state_dict['pos_embed'].shape)}  -> 8000 patches\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:49:05.475481Z\",\"iopub.execute_input\":\"2026-06-26T16:49:05.475776Z\",\"iopub.status.idle\":\"2026-06-26T16:49:05.484490Z\",\"shell.execute_reply.started\":\"2026-06-26T16:49:05.475736Z\",\"shell.execute_reply\":\"2026-06-26T16:49:05.483829Z\"}}\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\ndevice = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprint(f\"Device: {device}\")\n\n# ── How many blocks does this checkpoint have? ────────────────────────────────\n# Count unique block indices from the keys\nblock_indices = sorted(set(\n    int(k.split('.')[1])\n    for k in encoder_state_dict.keys()\n    if k.startswith(\"blocks.\")\n))\nNUM_LAYERS = len(block_indices)\nprint(f\"Number of EVA transformer blocks: {NUM_LAYERS}\")\nprint(f\"Block indices found: {block_indices[:5]} ... {block_indices[-3:]}\")\n\n# ── Architecture constants confirmed from checkpoint ─────────────────────────\nHIDDEN_SIZE  = 864\nNUM_HEADS    = 16    # 864 / 54 head_dim, standard for EVA-02\nPATCH_SIZE   = (8, 8, 8)\nTARGET_SIZE  = (160, 160, 160)\nN_PATCHES    = (TARGET_SIZE[0] // PATCH_SIZE[0]) ** 3  # 20^3 = 8000\nprint(f\"\\nConfirmed architecture:\")\nprint(f\"  hidden_size : {HIDDEN_SIZE}\")\nprint(f\"  num_layers  : {NUM_LAYERS}\")\nprint(f\"  patch_size  : {PATCH_SIZE}\")\nprint(f\"  input_size  : {TARGET_SIZE}\")\nprint(f\"  n_patches   : {N_PATCHES}  (should match pos_embed dim 8000)\")\nassert N_PATCHES == encoder_state_dict['pos_embed'].shape[1], \\\n    f\"Patch count mismatch: got {N_PATCHES}, pos_embed has {encoder_state_dict['pos_embed'].shape[1]}\"\nprint(f\"  pos_embed   : MATCHES checkpoint ✓\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:53:02.490095Z\",\"iopub.execute_input\":\"2026-06-26T16:53:02.490960Z\",\"iopub.status.idle\":\"2026-06-26T16:53:03.543496Z\",\"shell.execute_reply.started\":\"2026-06-26T16:53:02.490925Z\",\"shell.execute_reply\":\"2026-06-26T16:53:03.542489Z\"}}\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass EVASelfAttention(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16):\n        super().__init__()\n        self.num_heads = num_heads\n        self.head_dim  = hidden_size // num_heads\n        self.scale     = self.head_dim ** -0.5\n        self.q_proj    = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.k_proj    = nn.Linear(hidden_size, hidden_size, bias=False)\n        self.v_proj    = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.proj      = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.norm      = nn.LayerNorm(hidden_size)\n\n    def forward(self, x):\n        B, N, C = x.shape\n        q = self.q_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        k = self.k_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        v = self.v_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        attn = (q @ k.transpose(-2,-1)) * self.scale\n        attn = attn.softmax(dim=-1)\n        x = (attn @ v).transpose(1,2).reshape(B, N, C)\n        x = self.norm(x)\n        x = self.proj(x)\n        return x\n\n\nclass EVASwiGLUMLP(nn.Module):\n    def __init__(self, hidden_size=864, mlp_dim=2304):\n        super().__init__()\n        self.fc1_g = nn.Linear(hidden_size, mlp_dim, bias=True)\n        self.fc1_x = nn.Linear(hidden_size, mlp_dim, bias=True)\n        self.norm  = nn.LayerNorm(mlp_dim)\n        self.fc2   = nn.Linear(mlp_dim, hidden_size, bias=True)\n\n    def forward(self, x):\n        gate = F.silu(self.fc1_g(x))\n        x    = self.fc1_x(x)\n        x    = self.norm(gate * x)\n        x    = self.fc2(x)\n        return x\n\n\nclass EVABlock(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16, mlp_dim=2304):\n        super().__init__()\n        self.norm1   = nn.LayerNorm(hidden_size)\n        self.attn    = EVASelfAttention(hidden_size, num_heads)\n        self.norm2   = nn.LayerNorm(hidden_size)\n        self.mlp     = EVASwiGLUMLP(hidden_size, mlp_dim)\n        self.gamma_1 = nn.Parameter(torch.ones(hidden_size))\n        self.gamma_2 = nn.Parameter(torch.ones(hidden_size))\n\n    def forward(self, x):\n        x = x + self.gamma_1 * self.attn(self.norm1(x))\n        x = x + self.gamma_2 * self.mlp(self.norm2(x))\n        return x\n\n\nclass PRIMUSEVAEncoder(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16, mlp_dim=2304,\n                 num_layers=16, patch_size=(8,8,8),\n                 pretrain_n_patches=8000, pretrain_grid=(20,20,20)):\n        super().__init__()\n        self.stem      = nn.Conv3d(1, hidden_size, kernel_size=patch_size, stride=patch_size)\n        self.pos_embed = nn.Parameter(torch.zeros(1, pretrain_n_patches, hidden_size))\n        self.pretrain_grid = pretrain_grid\n        self.blocks    = nn.ModuleList([\n            EVABlock(hidden_size, num_heads, mlp_dim) for _ in range(num_layers)\n        ])\n        self.norm      = nn.LayerNorm(hidden_size)\n\n    def forward(self, x):\n        x = self.stem(x)\n        B, C, D, H, W = x.shape\n        n_patches = D * H * W\n        x = x.flatten(2).transpose(1, 2)\n\n        if n_patches != self.pos_embed.shape[1]:\n            gd, gh, gw = self.pretrain_grid\n            pos = self.pos_embed.transpose(1, 2)\n            pos = pos.reshape(1, C, gd, gh, gw)\n            pos = F.interpolate(pos, size=(D, H, W), mode=\"trilinear\", align_corners=False)\n            pos = pos.flatten(2).transpose(1, 2)\n        else:\n            pos = self.pos_embed\n\n        x = x + pos\n        for block in self.blocks:\n            x = block(x)\n        x = self.norm(x)\n        return x\n\n\nprimus_encoder = PRIMUSEVAEncoder(\n    hidden_size=864, num_heads=16, mlp_dim=2304, num_layers=16,\n    patch_size=(8, 8, 8), pretrain_n_patches=8000, pretrain_grid=(20, 20, 20),\n)\n\ntotal = sum(p.numel() for p in primus_encoder.parameters())\nprint(f\"PRIMUSEVAEncoder rebuilt with interpolation forward. Total params: {total/1e6:.1f} M\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:53:58.947193Z\",\"iopub.execute_input\":\"2026-06-26T16:53:58.947960Z\",\"iopub.status.idle\":\"2026-06-26T16:53:59.116610Z\",\"shell.execute_reply.started\":\"2026-06-26T16:53:58.947926Z\",\"shell.execute_reply\":\"2026-06-26T16:53:59.115823Z\"}}\nstem_renamed = {\n    \"weight\": stem_state_dict[\"proj.weight\"],\n    \"bias\":   stem_state_dict[\"proj.bias\"],\n}\nstem_missing, stem_unexpected = primus_encoder.stem.load_state_dict(stem_renamed, strict=True)\nprint(f\"Stem loaded — Missing: {stem_missing}, Unexpected: {stem_unexpected}\")\n\neva_missing, eva_unexpected = primus_encoder.load_state_dict(encoder_state_dict, strict=False)\nprint(f\"EVA encoder loaded — Missing: {len(eva_missing)}, Unexpected: {len(eva_unexpected)}\")\n\ndevice = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprimus_encoder = primus_encoder.to(device).eval()\nfor p in primus_encoder.parameters():\n    p.requires_grad = False\n\nprint(f\"Device: {device}\")\nprint(\"Encoder frozen and ready.\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:55:00.082820Z\",\"iopub.execute_input\":\"2026-06-26T16:55:00.083694Z\",\"iopub.status.idle\":\"2026-06-26T16:55:00.897716Z\",\"shell.execute_reply.started\":\"2026-06-26T16:55:00.083660Z\",\"shell.execute_reply\":\"2026-06-26T16:55:00.896983Z\"}}\nclass PrimusClassifier(nn.Module):\n    \"\"\"\n    Frozen PRIMUS EVA encoder + trainable classification head.\n    Uses mean pooling over all patch tokens (no CLS token in this checkpoint).\n    \"\"\"\n    def __init__(self, encoder, hidden_size=864, num_classes=3):\n        super().__init__()\n        self.encoder = encoder                          # frozen\n        self.head = nn.Sequential(\n            nn.LayerNorm(hidden_size),\n            nn.Linear(hidden_size, num_classes)         # 864 -> 3\n        )\n\n    def forward(self, x):\n        patch_tokens = self.encoder(x)                  # (B, 1728, 864)\n        pooled       = patch_tokens.mean(dim=1)         # (B, 864)\n        return self.head(pooled)                        # (B, 3)\n\n\nmodel = PrimusClassifier(primus_encoder, hidden_size=864, num_classes=3)\nmodel = model.to(device)\n\nenc_params  = sum(p.numel() for p in model.encoder.parameters())\nhead_params = sum(p.numel() for p in model.head.parameters() if p.requires_grad)\nprint(f\"Encoder params (frozen)  : {enc_params/1e6:.1f} M\")\nprint(f\"Head params (trainable)  : {head_params:,}\")\n\nprint(\"\\nRunning shape test ...\")\nwith torch.no_grad():\n    dummy = torch.randn(1, 1, 96, 96, 96).to(device)\n    out   = model(dummy)\n    print(f\"  Input  : {tuple(dummy.shape)}\")\n    print(f\"  Output : {tuple(out.shape)}\")\n    assert out.shape == (1, 3), f\"Expected (1,3) got {out.shape}\"\n    print(\"  Shape test passed - ready for feature extraction\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:55:39.861137Z\",\"iopub.execute_input\":\"2026-06-26T16:55:39.861901Z\",\"iopub.status.idle\":\"2026-06-26T16:55:39.868786Z\",\"shell.execute_reply.started\":\"2026-06-26T16:55:39.861869Z\",\"shell.execute_reply\":\"2026-06-26T16:55:39.867860Z\"}}\n@torch.no_grad()\ndef extract_features(loader):\n    primus_encoder.eval()\n    feats, labels = [], []\n\n    for batch in loader:\n        x = batch[\"img\"][tio.DATA].to(device)   # (B, 1, 96, 96, 96)\n        y = batch[\"diagnosis\"]                   # (B,)\n\n        batch_feats = []\n        for i in range(x.shape[0]):\n            xi           = x[i].unsqueeze(0)         # (1, 1, 96, 96, 96)\n            patch_tokens = primus_encoder(xi)         # (1, 1728, 864)\n            pooled       = patch_tokens.mean(dim=1)  # (1, 864)\n            batch_feats.append(pooled.cpu())\n\n        feats.append(torch.cat(batch_feats, dim=0))\n        labels.append(y)\n\n    return torch.cat(feats).numpy(), torch.cat(labels).numpy()\n\nprint(\"extract_features() defined (one-subject-at-a-time, OOM-safe)\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:56:26.409573Z\",\"iopub.execute_input\":\"2026-06-26T16:56:26.410515Z\",\"iopub.status.idle\":\"2026-06-26T16:58:34.588969Z\",\"shell.execute_reply.started\":\"2026-06-26T16:56:26.410479Z\",\"shell.execute_reply\":\"2026-06-26T16:58:34.587068Z\"}}\nfold_scores = []\n\nfor fold in range(N_FOLDS):\n    val_start  = fold * fold_size\n    val_end    = val_start + fold_size if fold < N_FOLDS - 1 else len(perm)\n    val_idx    = perm[val_start:val_end].tolist()\n    tr_idx     = torch.cat([perm[:val_start], perm[val_end:]]).tolist()\n\n    fold_train = [subjects[i] for i in tr_idx]\n    fold_val   = [subjects[i] for i in val_idx]\n\n    train_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_train, transform=transform),\n        batch_size=2, shuffle=False, num_workers=0\n    )\n    val_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_val, transform=transform),\n        batch_size=2, shuffle=False, num_workers=0\n    )\n\n    print(f\"[Fold {fold+1}] Extracting train features ({len(fold_train)} subjects)...\")\n    X_train, y_train = extract_features(train_loader)\n\n    print(f\"[Fold {fold+1}] Extracting val features ({len(fold_val)} subjects)...\")\n    X_val, y_val = extract_features(val_loader)\n\n    clf   = LogisticRegression(max_iter=2000, class_weight=\"balanced\")\n    clf.fit(X_train, y_train)\n    preds = clf.predict(X_val)\n\n    bal_acc  = balanced_accuracy_score(y_val, preds)\n    macro_f1 = f1_score(y_val, preds, average=\"macro\")\n    fold_scores.append((bal_acc, macro_f1))\n    print(f\"[Fold {fold+1}] balanced_acc={bal_acc:.3f}  macro_f1={macro_f1:.3f}\\n\")\n\nscores = np.array(fold_scores)\nprint(\"=\" * 50)\nprint(\"  PRIMUS -- 5-fold CV results\")\nprint(\"=\" * 50)\nprint(f\"  balanced_acc : {scores[:,0].mean():.3f} +/- {scores[:,0].std():.3f}\")\nprint(f\"  macro_f1     : {scores[:,1].mean():.3f} +/- {scores[:,1].std():.3f}\")\nprint(\"=\" * 50)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T17:01:28.681507Z\",\"iopub.execute_input\":\"2026-06-26T17:01:28.682016Z\",\"iopub.status.idle\":\"2026-06-26T17:01:58.345562Z\",\"shell.execute_reply.started\":\"2026-06-26T17:01:28.681982Z\",\"shell.execute_reply\":\"2026-06-26T17:01:58.344860Z\"}}\nfull_loader = tio.SubjectsLoader(\n    tio.SubjectsDataset(train_val_subjs, transform=transform),\n    batch_size=2, shuffle=False, num_workers=0\n)\n\nprint(\"Extracting features for full train+val set...\")\nX_full, y_full = extract_features(full_loader)\n\nprint(\"Extracting features for held-out test set...\")\nX_test, y_test = extract_features(test_loader)\n\nfinal_clf = LogisticRegression(max_iter=2000, class_weight=\"balanced\")\nfinal_clf.fit(X_full, y_full)\ntest_preds = final_clf.predict(X_test)\n\nprint(\"\\n\" + \"=\" * 50)\nprint(\"  PRIMUS -- Held-out test performance\")\nprint(\"=\" * 50)\nprint(f\"  Balanced accuracy : {balanced_accuracy_score(y_test, test_preds):.3f}\")\nprint(f\"  Macro F1          : {f1_score(y_test, test_preds, average='macro'):.3f}\")\nprint(\"=\" * 50)\nprint(\"\\nBrainIAC reference (from benchmark notebook):\")\nprint(\"  Balanced accuracy : 0.667\")\nprint(\"  Macro F1          : 0.508\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T17:06:23.100610Z\",\"iopub.execute_input\":\"2026-06-26T17:06:23.101367Z\",\"iopub.status.idle\":\"2026-06-26T17:06:23.141833Z\",\"shell.execute_reply.started\":\"2026-06-26T17:06:23.101334Z\",\"shell.execute_reply\":\"2026-06-26T17:06:23.140834Z\"}}\nfrom sklearn.metrics import classification_report, confusion_matrix, accuracy_score, f1_score\n\n# Map back from integer labels to class names\ninv_label_map = {v: k for k, v in LABEL_MAP.items()}\ntarget_names = [inv_label_map[i] for i in sorted(inv_label_map)]\n\nprint(\"=\" * 60)\nprint(\"  PRIMUS — Full test set breakdown\")\nprint(\"=\" * 60)\n\n# Overall accuracy (unweighted, raw accuracy)\nacc = accuracy_score(y_test, test_preds)\nprint(f\"Test accuracy (raw)     : {acc:.3f}\")\n\n# Weighted F1 (accounts for class imbalance, weights by support)\nf1_weighted = f1_score(y_test, test_preds, average=\"weighted\")\nprint(f\"Test F1 (weighted)      : {f1_weighted:.3f}\")\n\nf1_macro = f1_score(y_test, test_preds, average=\"macro\")\nprint(f\"Test F1 (macro)         : {f1_macro:.3f}\")\n\nprint(\"\\nPer-class precision / recall / F1:\")\nprint(classification_report(y_test, test_preds, target_names=target_names, digits=3))\n\nprint(\"Confusion matrix (rows = true, cols = predicted):\")\ncm = confusion_matrix(y_test, test_preds)\ncm_df = pd.DataFrame(cm, index=target_names, columns=target_names)\nprint(cm_df)\nprint(\"=\" * 60)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:49:11.044868Z\",\"iopub.execute_input\":\"2026-06-26T16:49:11.045470Z\",\"iopub.status.idle\":\"2026-06-26T16:49:12.205999Z\",\"shell.execute_reply.started\":\"2026-06-26T16:49:11.045438Z\",\"shell.execute_reply\":\"2026-06-26T16:49:12.205287Z\"}}\nclass EVASelfAttention(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16):\n        super().__init__()\n        self.num_heads  = num_heads\n        self.head_dim   = hidden_size // num_heads\n        self.scale      = self.head_dim ** -0.5\n        self.q_proj     = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.k_proj     = nn.Linear(hidden_size, hidden_size, bias=False)\n        self.v_proj     = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.proj       = nn.Linear(hidden_size, hidden_size, bias=True)\n        self.norm       = nn.LayerNorm(hidden_size)\n\n    def forward(self, x):\n        B, N, C = x.shape\n        q = self.q_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        k = self.k_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        v = self.v_proj(x).reshape(B, N, self.num_heads, self.head_dim).transpose(1,2)\n        attn = (q @ k.transpose(-2,-1)) * self.scale\n        attn = attn.softmax(dim=-1)\n        x = (attn @ v).transpose(1,2).reshape(B, N, C)\n        x = self.norm(x)\n        x = self.proj(x)\n        return x\n\n\nclass EVASwiGLUMLP(nn.Module):\n    def __init__(self, hidden_size=864, mlp_dim=2304):\n        super().__init__()\n        self.fc1_g = nn.Linear(hidden_size, mlp_dim, bias=True)\n        self.fc1_x = nn.Linear(hidden_size, mlp_dim, bias=True)\n        self.norm   = nn.LayerNorm(mlp_dim)\n        self.fc2    = nn.Linear(mlp_dim, hidden_size, bias=True)\n\n    def forward(self, x):\n        gate = F.silu(self.fc1_g(x))\n        x    = self.fc1_x(x)\n        x    = self.norm(gate * x)\n        x    = self.fc2(x)\n        return x\n\n\nclass EVABlock(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16, mlp_dim=2304):\n        super().__init__()\n        self.norm1   = nn.LayerNorm(hidden_size)\n        self.attn    = EVASelfAttention(hidden_size, num_heads)\n        self.norm2   = nn.LayerNorm(hidden_size)\n        self.mlp     = EVASwiGLUMLP(hidden_size, mlp_dim)\n        self.gamma_1 = nn.Parameter(torch.ones(hidden_size))\n        self.gamma_2 = nn.Parameter(torch.ones(hidden_size))\n\n    def forward(self, x):\n        x = x + self.gamma_1 * self.attn(self.norm1(x))\n        x = x + self.gamma_2 * self.mlp(self.norm2(x))\n        return x\n\n\nclass PRIMUSEVAEncoder(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16, mlp_dim=2304,\n                 num_layers=16, patch_size=(8,8,8)):   # num_layers=16 confirmed\n        super().__init__()\n        self.stem      = nn.Conv3d(1, hidden_size,\n                                   kernel_size=patch_size, stride=patch_size)\n        self.pos_embed = nn.Parameter(torch.zeros(1, 8000, hidden_size))\n        self.blocks    = nn.ModuleList([\n            EVABlock(hidden_size, num_heads, mlp_dim)\n            for _ in range(num_layers)\n        ])\n        self.norm = nn.LayerNorm(hidden_size)\n\n    def forward(self, x):\n        x = self.stem(x)                        # (B, 864, 20, 20, 20)\n        B, C, D, H, W = x.shape\n        x = x.flatten(2).transpose(1, 2)        # (B, 8000, 864)\n        x = x + self.pos_embed\n        for block in self.blocks:\n            x = block(x)\n        x = self.norm(x)                        # (B, 8000, 864)\n        return x\n\n\n# Build with confirmed num_layers=16\nprimus_encoder = PRIMUSEVAEncoder(\n    hidden_size=864,\n    num_heads=16,\n    mlp_dim=2304,\n    num_layers=16,           # confirmed from Cell 10 output\n    patch_size=(8, 8, 8),\n)\n\ntotal = sum(p.numel() for p in primus_encoder.parameters())\nprint(f\"PRIMUSEVAEncoder built — {total/1e6:.1f} M parameters\")\nprint(f\"Expected ~16 blocks x ~864 hidden = roughly 200-300M params\")\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:49:23.756942Z\",\"iopub.execute_input\":\"2026-06-26T16:49:23.757661Z\",\"iopub.status.idle\":\"2026-06-26T16:49:24.364135Z\",\"shell.execute_reply.started\":\"2026-06-26T16:49:23.757629Z\",\"shell.execute_reply\":\"2026-06-26T16:49:24.363310Z\"}}\n# Load stem weights\nstem_renamed = {\n    \"weight\": stem_state_dict[\"proj.weight\"],\n    \"bias\":   stem_state_dict[\"proj.bias\"],\n}\nstem_missing, stem_unexpected = primus_encoder.stem.load_state_dict(\n    stem_renamed, strict=True\n)\nprint(f\"Stem loaded — missing: {stem_missing}, unexpected: {stem_unexpected}\")\n\n# Load EVA encoder weights (pos_embed + all blocks + final norm)\neva_missing, eva_unexpected = primus_encoder.load_state_dict(\n    encoder_state_dict, strict=False\n)\n\nprint(f\"\\nEVA encoder weights loaded:\")\nprint(f\"  Missing keys    : {len(eva_missing)}\")\nprint(f\"  Unexpected keys : {len(eva_unexpected)}\")\n\nif eva_missing:\n    print(f\"  Missing sample  : {eva_missing[:5]}\")\nif eva_unexpected:\n    print(f\"  Unexpected sample: {eva_unexpected[:5]}\")\n\n# Freeze and move to device\nprimus_encoder = primus_encoder.to(device).eval()\nfor p in primus_encoder.parameters():\n    p.requires_grad = False\n\nfrozen = sum(p.numel() for p in primus_encoder.parameters())\nprint(f\"\\nFrozen parameters : {frozen/1e6:.1f} M\")\nprint(\"Encoder ready.\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T16:50:11.639641Z\",\"iopub.execute_input\":\"2026-06-26T16:50:11.640442Z\",\"iopub.status.idle\":\"2026-06-26T16:50:12.664027Z\",\"shell.execute_reply.started\":\"2026-06-26T16:50:11.640406Z\",\"shell.execute_reply\":\"2026-06-26T16:50:12.662858Z\"}}\nclass PrimusClassifier(nn.Module):\n    \"\"\"\n    Frozen PRIMUS EVA encoder + trainable classification head.\n    Uses mean pooling over all patch tokens (no CLS token in this checkpoint).\n    \"\"\"\n    def __init__(self, encoder, hidden_size=864, num_classes=3):\n        super().__init__()\n        self.encoder = encoder                          # frozen\n        self.head = nn.Sequential(\n            nn.LayerNorm(hidden_size),\n            nn.Linear(hidden_size, num_classes)         # 864 -> 3\n        )\n\n    def forward(self, x):\n        patch_tokens = self.encoder(x)                  # (B, 1728, 864)\n        pooled       = patch_tokens.mean(dim=1)         # (B, 864)\n        return self.head(pooled)                        # (B, 3)\n\n\nmodel = PrimusClassifier(primus_encoder, hidden_size=864, num_classes=3)\nmodel = model.to(device)\n\nenc_params  = sum(p.numel() for p in model.encoder.parameters())\nhead_params = sum(p.numel() for p in model.head.parameters() if p.requires_grad)\nprint(f\"Encoder params (frozen)  : {enc_params/1e6:.1f} M\")\nprint(f\"Head params (trainable)  : {head_params:,}\")\n\nprint(\"\\nRunning shape test ...\")\nwith torch.no_grad():\n    dummy = torch.randn(1, 1, 96, 96, 96).to(device)\n    out   = model(dummy)\n    print(f\"  Input  : {tuple(dummy.shape)}\")\n    print(f\"  Output : {tuple(out.shape)}\")\n    assert out.shape == (1, 3), f\"Expected (1,3) got {out.shape}\"\n    print(\"  Shape test passed - ready for feature extraction\")\n\n# %% [code]\n\n\n# %% [code]\nimport torch.nn.functional as F\n\nprint(\"Running shape test on CPU with 96^3 input ...\")\nwith torch.no_grad():\n    dummy = torch.randn(1, 1, 96, 96, 96).to(device)\n    out = primus_encoder(dummy)   # should be (1, 1728, 864)\n    print(f\"  Encoder output: {tuple(out.shape)}\")\n    assert out.shape == (1, 1728, 864), f\"Unexpected shape: {out.shape}\"\n    print(\"  Shape test passed ✓\")\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T12:49:46.946250Z\",\"iopub.execute_input\":\"2026-06-26T12:49:46.946980Z\",\"iopub.status.idle\":\"2026-06-26T12:49:46.952840Z\",\"shell.execute_reply.started\":\"2026-06-26T12:49:46.946940Z\",\"shell.execute_reply\":\"2026-06-26T12:49:46.951747Z\"}}\ndef forward(self, x):\n    x = self.stem(x)                          # (B, 864, 12, 12, 12) for 96^3 input\n    B, C, D, H, W = x.shape\n    n_patches = D * H * W                     # 1728 for 96^3\n\n    x = x.flatten(2).transpose(1, 2)         # (B, 1728, 864)\n\n    # Interpolate pos_embed from 8000 -> 1728 patches\n    if n_patches != self.pos_embed.shape[1]:\n        pos = self.pos_embed.transpose(1, 2)  # (1, 864, 8000)\n        pos = pos.reshape(1, C, 20, 20, 20)   # (1, 864, 20, 20, 20)\n        pos = F.interpolate(\n            pos, size=(D, H, W),\n            mode=\"trilinear\", align_corners=False\n        )                                      # (1, 864, 12, 12, 12)\n        pos = pos.flatten(2).transpose(1, 2)  # (1, 1728, 864)\n    else:\n        pos = self.pos_embed\n\n    x = x + pos\n    for block in self.blocks:\n        x = block(x)\n    x = self.norm(x)\n    return x                                   # (B, 1728, 864)\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T12:50:10.286439Z\",\"iopub.execute_input\":\"2026-06-26T12:50:10.286970Z\",\"iopub.status.idle\":\"2026-06-26T12:50:10.294692Z\",\"shell.execute_reply.started\":\"2026-06-26T12:50:10.286889Z\",\"shell.execute_reply\":\"2026-06-26T12:50:10.293891Z\"}}\nclass PRIMUSEVAEncoder(nn.Module):\n    def __init__(self, hidden_size=864, num_heads=16, mlp_dim=2304,\n                 num_layers=16, patch_size=(8,8,8)):\n        super().__init__()\n        self.stem      = nn.Conv3d(1, hidden_size,\n                                   kernel_size=patch_size, stride=patch_size)\n        self.pos_embed = nn.Parameter(torch.zeros(1, 8000, hidden_size))\n        self.blocks    = nn.ModuleList([\n            EVABlock(hidden_size, num_heads, mlp_dim)\n            for _ in range(num_layers)\n        ])\n        self.norm      = nn.LayerNorm(hidden_size)\n        self.patch_size = patch_size\n\n    def forward(self, x):\n        x = self.stem(x)                         # (B, 864, D', H', W')\n        B, C, D, H, W = x.shape\n        n_patches = D * H * W\n        x = x.flatten(2).transpose(1, 2)         # (B, n_patches, 864)\n\n        # Interpolate pos_embed if input patch count differs from pretraining\n        if n_patches != self.pos_embed.shape[1]:\n            # reshape pretrained pos_embed from (1, 8000, 864) to (1, 864, 20, 20, 20)\n            # then interpolate to (1, 864, D, H, W)\n            pos = self.pos_embed.transpose(1, 2)  # (1, 864, 8000)\n            pos = pos.reshape(1, C, 20, 20, 20)   # (1, 864, 20, 20, 20)\n            pos = F.interpolate(\n                pos, size=(D, H, W),\n                mode=\"trilinear\", align_corners=False\n            )                                      # (1, 864, D, H, W)\n            pos = pos.flatten(2).transpose(1, 2)  # (1, n_patches, 864)\n        else:\n            pos = self.pos_embed\n\n        x = x + pos\n        for block in self.blocks:\n            x = block(x)\n        x = self.norm(x)\n        return x\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T12:50:14.840220Z\",\"iopub.execute_input\":\"2026-06-26T12:50:14.840493Z\",\"iopub.status.idle\":\"2026-06-26T12:50:14.850394Z\",\"shell.execute_reply.started\":\"2026-06-26T12:50:14.840471Z\",\"shell.execute_reply\":\"2026-06-26T12:50:14.849315Z\"}}\nclass PrimusClassifier(nn.Module):\n    \"\"\"\n    Frozen PRIMUS EVA encoder + trainable classification head.\n    No CLS token in this MAE checkpoint — uses mean pooling instead.\n\n    Input  : (B, 1, 160, 160, 160)\n    Output : (B, 3)  scores for Control / Dementia / Parkinson\n    \"\"\"\n    def __init__(self, encoder, hidden_size=864, num_classes=3):\n        super().__init__()\n        self.encoder = encoder\n        self.head = nn.Sequential(\n            nn.LayerNorm(hidden_size),\n            nn.Linear(hidden_size, num_classes)\n        )\n\n    def forward(self, x):\n        patch_tokens = self.encoder(x)          # (B, 8000, 864)\n        pooled       = patch_tokens.mean(dim=1) # (B, 864) — mean over all patches\n        return self.head(pooled)                # (B, 3)\n\n\nmodel = PrimusClassifier(primus_encoder, hidden_size=864, num_classes=3)\nmodel = model.to(device)\n\nhead_params = sum(p.numel() for p in model.head.parameters() if p.requires_grad)\nenc_params  = sum(p.numel() for p in model.encoder.parameters())\nprint(f\"Encoder params (frozen)    : {enc_params/1e6:.1f} M\")\nprint(f\"Head params (trainable)    : {head_params:,}\")\nprint(f\"Input expected             : (B, 1, 160, 160, 160)\")\nprint(f\"Output                     : (B, 3)\")\n\n# Shape test — confirms the full forward pass works before running on real data\nprint(\"\\nRunning shape test ...\")\nwith torch.no_grad():\n    dummy = torch.randn(1, 1, 160, 160, 160).to(device)\n    out   = model(dummy)\n    print(f\"  Input  : {tuple(dummy.shape)}\")\n    print(f\"  Output : {tuple(out.shape)}\")\n    print(f\"  Shape test passed ✓\" if out.shape == (1, 3) else \"  Shape test FAILED\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T12:06:02.951522Z\",\"iopub.execute_input\":\"2026-06-26T12:06:02.951956Z\",\"iopub.status.idle\":\"2026-06-26T12:06:02.961401Z\",\"shell.execute_reply.started\":\"2026-06-26T12:06:02.951900Z\",\"shell.execute_reply\":\"2026-06-26T12:06:02.960332Z\"}}\n@torch.no_grad()\ndef extract_features(loader) -> tuple[np.ndarray, np.ndarray]:\n    \"\"\"\n    Pass all batches in `loader` through the frozen PRIMUS encoder.\n    Returns:\n        features : np.ndarray of shape (N, 768)  — CLS token per subject\n        labels   : np.ndarray of shape (N,)       — integer class labels\n    \"\"\"\n    primus_encoder.eval()   # make sure encoder stays in eval mode\n    feats, labels = [], []\n\n    for batch in loader:\n        # batch[\"img\"][tio.DATA] shape: (B, 1, 96, 96, 96)\n        x = batch[\"img\"][tio.DATA].to(device)\n        y = batch[\"diagnosis\"]                  # (B,) integer labels\n\n        out = primus_encoder(x)                 # (B, 217, 768) — or tuple\n        if isinstance(out, (tuple, list)):\n            out = out[0]\n\n        cls_token = out[:, 0]                   # (B, 768)  CLS token\n        feats.append(cls_token.cpu())\n        labels.append(y)\n\n    return torch.cat(feats).numpy(), torch.cat(labels).numpy()\n\n\nprint(\"extract_features() defined.\")\nprint(\"  Input  per batch : (B, 1, 96, 96, 96)\")\nprint(\"  Output per batch : (B, 768) CLS tokens\")\n\n# %% [code] {\"execution\":{\"iopub.status.busy\":\"2026-06-26T12:06:10.716080Z\",\"iopub.execute_input\":\"2026-06-26T12:06:10.716955Z\",\"execution_failed\":\"2026-06-26T12:06:39.473Z\"}}\nfold_scores = []\n\nfor fold in range(N_FOLDS):\n    val_start  = fold * fold_size\n    val_end    = val_start + fold_size if fold < N_FOLDS - 1 else len(perm)\n    val_idx    = perm[val_start:val_end].tolist()\n    tr_idx     = torch.cat([perm[:val_start], perm[val_end:]]).tolist()\n\n    fold_train = [subjects[i] for i in tr_idx]\n    fold_val   = [subjects[i] for i in val_idx]\n\n    train_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_train, transform=transform),\n        batch_size=BATCH_SIZE, shuffle=False, num_workers=0\n    )\n    val_loader = tio.SubjectsLoader(\n        tio.SubjectsDataset(fold_val, transform=transform),\n        batch_size=BATCH_SIZE, shuffle=False, num_workers=0\n    )\n\n    print(f\"[Fold {fold+1}] Extracting features ...\")\n    X_train, y_train = extract_features(train_loader)\n    X_val,   y_val   = extract_features(val_loader)\n\n    clf   = LogisticRegression(max_iter=2000, class_weight=\"balanced\")\n    clf.fit(X_train, y_train)\n    preds = clf.predict(X_val)\n\n    bal_acc  = balanced_accuracy_score(y_val, preds)\n    macro_f1 = f1_score(y_val, preds, average=\"macro\")\n    fold_scores.append((bal_acc, macro_f1))\n    print(f\"[Fold {fold+1}] balanced_acc={bal_acc:.3f}  macro_f1={macro_f1:.3f}\\n\")\n\nscores = np.array(fold_scores)\nprint(\"=\" * 50)\nprint(\"  PRIMUS — 5-fold CV results\")\nprint(\"=\" * 50)\nprint(f\"  balanced_acc : {scores[:,0].mean():.3f} ± {scores[:,0].std():.3f}\")\nprint(f\"  macro_f1     : {scores[:,1].mean():.3f} ± {scores[:,1].std():.3f}\")\nprint(\"=\" * 50)\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n\n\n# %% [code]\n","metadata":{"_uuid":"74fc4958-39a4-4932-97ee-057242c4d1aa","_cell_guid":"6b1f2295-a97b-4757-830d-36d0a9d632ba","trusted":true,"collapsed":false,"jupyter":{"outputs_hidden":false}},"outputs":[],"execution_count":null}]}